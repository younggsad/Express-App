info:
  name: GETW4
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/users?search=example.com
  params:
    - name: search
      value: example.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
