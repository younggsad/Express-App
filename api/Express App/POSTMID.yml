info:
  name: POSTMID
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/users
  body:
    type: json
    data: |-
      {
        "name": "A",
        "email": "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
