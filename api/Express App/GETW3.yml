info:
  name: GETW3
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/users?search=alex
  params:
    - name: search
      value: alex
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
