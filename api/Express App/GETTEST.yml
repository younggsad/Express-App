info:
  name: GETTEST
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/users?page=2&limit=5&sort=name&order=desc&search=alex
  params:
    - name: page
      value: "2"
      type: query
    - name: limit
      value: "5"
      type: query
    - name: sort
      value: name
      type: query
    - name: order
      value: desc
      type: query
    - name: search
      value: alex
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
